[phases.setup]
# better-sqlite3 native derleme ve canvas metinleri (font) için gerekli araçlar
nixPkgs = ["python3", "gcc", "gnumake", "fontconfig", "dejavu_fonts", "liberation_ttf"]

[phases.install]
cmds = ["npm ci --build-from-source"]

[start]
cmd = "node index.js"
